{
  "name": "aqarati-pro",
  "version": "1.0.0",
  "description": "عقاراتي Pro - Smart Property Management",
  "scripts": {
    "build:android": "npx cap add android && npx cap sync && npx cap build android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}